# Local reverse proxy or Cloudflare Tunnel on the VM:
SPRAKVASK_BIND=127.0.0.1
SPRAKVASK_PORT=3544
SPRAKVASK_IMAGE=sprakvask-site:local

# To expose HTTP directly on the VM instead:
# SPRAKVASK_BIND=0.0.0.0
# SPRAKVASK_PORT=80
